var subject = "coding"; //Using double quotes
var val = 'Hi';//Using single quotes
console.log(subject);
console.log(val);

let firstname = "Udit"; //Using double quotes
let lastname = `Jangra`; //Using backticks

let fullname = firstname + " " + lastname; //Using backticks for template literals
console.log(fullname);

let fullname2 = `${firstname} ${lastname}`; //Using backticks for template literals
console.log(fullname2);